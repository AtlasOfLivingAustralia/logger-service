services:
  mysql:
    container_name: logger-mysql
    image: "mysql:8.1.0"
    environment:
      MYSQL_DATABASE: 'logger'
      MYSQL_USER:  'logger'
      MYSQL_PASSWORD: 'password'
      MYSQL_ROOT_PASSWORD: 'password'
    command: --default-authentication-plugin=mysql_native_password --connect_timeout=1200
    ports:
      - "3306:3306"
    expose:
      - '3306'
version: "2"